---
name: docs
description: Порядок обновления документации проекта АРМ — контракт протокола PF60, описание, README, отчёты в docs. Применяется, когда изменение затронуло протокол, сборку или структуру проекта.
disable-model-invocation: true
---

# Документация проекта

## Карта документов

| Документ | Что в нём | Когда правится |
|----------|-----------|----------------|
| `protocol/documentation/current_protocol.md` | контракт обмена: поля, размеры, диапазоны, версии схемы | до правки кода протокола |
| `protocol/documentation/PF60_описание.md` | объяснение протокола: зачем так, типовой обмен | после изменения контракта |
| `README.md` | назначение, сборка, структура каталогов | при изменении сборки, зависимостей, раскладки |
| `docs/plans/` | планы работ от `planner` | при запуске `/orchestrate` |
| `docs/reports/` | итоговые отчёты и аудиты | по завершении работы |

Имя файла в `docs/`: `YYYY-MM-DD-краткое-имя.md`.

## Порядок при изменении протокола

Только в такой последовательности, иначе код и контракт расходятся:

1. `protocol/documentation/current_protocol.md` — описать новое поле или кадр как контракт.
2. `protocol/pf60_protocol.h/.cpp` — реализовать.
3. `tests/test_protocol.cpp` — покрыть корректный, битый и граничный кадр.
4. `protocol/documentation/PF60_описание.md` — объяснить, если поведение неочевидно.
5. `README.md` — только если изменился порядок работы с изделием.

## Правила

- Русский язык. Пишем для инженера, который вернётся через полгода.
- Ссылаемся на файлы и функции, код не копируем: копия протухает молча.
- Неверная документация вреднее отсутствующей. Нашёл расхождение с кодом —
  чини его первым, даже если это не входило в задачу.
- Правка на одну строку в README не требует отдельного отчёта в `docs/`.
- Не описываем очевидное и не пересказываем diff.

## Шаблон отчёта

```markdown
# <Задача>

## Что сделано
<2–4 предложения по сути>

## Изменённые файлы
- `путь` — суть правки

## Как проверено
<команды и результат>

## Что осталось
- <незакрытое и почему>
```
